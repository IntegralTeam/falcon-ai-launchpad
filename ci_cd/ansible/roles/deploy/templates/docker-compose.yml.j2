networks:
  website:
    external: true
    name: website

services:
  {{ CI_PROJECT_NAME }}:
    image: {{ CI_REGISTRY_IMAGE }}:{{ CI_COMMIT_REF_NAME }}
    container_name: falcon-ai-launchpad-{{ CI_PROJECT_NAME }}
    ports:
      - "8888:4173"
    restart: always
    networks:
      - website